# AI News Parser & Translator Bot

**AI News Parser** — это мощная система для автоматического сбора, перевода и публикации новостей об искусственном интеллекте. Приложение собирает контент с ведущих технологических изданий (**TechCrunch**, **The Verge**, **VentureBeat**), переводит его на русский язык с помощью локального нейросетевого переводчика и позволяет отправлять отобранные новости в Telegram-канал.

![Python](https://img.shields.io/badge/Python-3.11-blue)
![FastAPI](https://img.shields.io/badge/FastAPI-0.109-009688)
![Playwright](https://img.shields.io/badge/Playwright-Stopping%20Power-orange)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED)

---

## ✨ Ключевые Возможности

### 🔍 Умный Парсинг (Scraping)
- **Многопоточный сбор**: Использует **Playwright** (Headless Chromium) для работы с современными SPA-сайтами.
- **Источники**:
  - [TechCrunch AI](https://techcrunch.com/category/artificial-intelligence/)
  - [The Verge AI](https://www.theverge.com/ai-artificial-intelligence)
  - [VentureBeat AI](https://venturebeat.com/category/ai/)
- **Проверка доступности**: Система автоматически проверяет доступность каждого источника и помечает их статус (🟢 Online / 🔴 Offline).

### 🧠 Нейросетевой Перевод
- Интеграция с локальным instance **LibreTranslate** (запускается в отдельном контейнере).
- Автоматический перевод заголовков и кратких содержаний (summary) с английского на русский.
- Fallback-режим: если переводчик недоступен, новость сохраняется на оригинальном языке.

### 📊 Детальная Статистика
- **Дедупликация**: Умная проверка на дубликаты по заголовкам новостей.
- **Отчетность**: Подробный отчет после каждого цикла парсинга:
  - Сколько новостей **найдено** на каждом сайте.
  - Сколько **добавлено** в базу.
  - Сколько **пропущено** (дубликаты).

### 🤖 Автоматизация
- **Фоновый режим**: Возможность запуска цикла авто-парсинга каждые 30 секунд.
- **Лимиты**: Интеллектуальное управление очередью (авто-пауза, если в базе >10 неопубликованных новостей).

---

## 🛠 Технологический Стек

| Компонент | Технология | Описание |
|-----------|------------|----------|
| **Backend** | Python, FastAPI | Высокопроизводительный асинхронный API. |
| **База данных** | aiosqlite | Асинхронная работа с SQLite. |
| **Scraper** | Playwright | Эмуляция браузера для обхода защиты от ботов. |
| **Переводчик** | LibreTranslate | Офлайн API перевода (Docker). |
| **Frontend** | HTML/JS/CSS | Адаптивный веб-интерфейс для управления. |
| **Деплой** | Docker Compose | Оркестрация контейнеров (App + Translator). |

---

## 🚀 Установка и Запуск

### Предварительные требования
- Установленный **Docker** и **Docker Compose**.

### 1. Клонирование репозитория
```bash
git clone https://github.com/kolt2050/news_parsing_eng_bot.git
cd news_parsing_eng_bot
```

### 2. Настройка окружения
В проекте используются переменные окружения для хранения секретов. 

Создайте файл `.env` в корне проекта и добавьте туда данные вашего Telegram бота:
```bash
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=id_вашего_канала
```
> **Как получить токен?** Напишите [@BotFather](https://t.me/BotFather) в Telegram.
> **Как узнать Chat ID?** Перешлите любое сообщение из канала боту [@userinfobot](https://t.me/userinfobot) или используйте API.

### 3. Запуск
```bash
docker-compose up -d --build
```
*Первый запуск может занять несколько минут, так как будет скачиваться модель перевода.*

### 4. Доступ к интерфейсу
Откройте браузер и перейдите по адресу:
👉 **http://localhost:8000**

---

## 📖 Руководство Пользователя

### Главная Панель (Dashboard)
Веб-интерфейс разделен на несколько зон:

1.  **Статус бар (Вверху)**:
    - Показывает состояние сервисов (API, Переводчик).
    - Отображает статистику последнего парсинга.
    - **Совет**: Наведите курсор на "Поиск новостей", чтобы увидеть детальную статистику по каждому сайту.

2.  **Панель Управления (Слева)**:
    - **Парсить новости (Manual)**: Ручной запуск сбора новостей.
    - **Авто-парсинг (Swith)**: Включает бесконечный цикл сбора новостей в фоне.
    - **Очистить все**: Удаляет все новости из базы данных.

3.  **Список Новостей (Центр)**:
    - Карточки с новостями.
    - Значки источников (TechCrunch, The Verge и т.д.).
    - Кнопки действий:
        - **Отправить**: Публикация конкретной новости в Telegram.
        - **Удалить**: Удаление новости из базы.

4.  **Счетчики (Справа)**:
    - **В ожидании**: Новости, готовые к отправке.
    - **Отправлено**: Статистика опубликованных постов.

---

## 📡 API Reference

Приложение предоставляет REST API для интеграции.

### Основные Эндпоинты

| Метод | URL | Описание |
|-------|-----|----------|
| `GET` | `/api/status` | Получить статус сервисов и статистику. |
| `GET` | `/api/news` | Получить список всех новостей в базе. |
| `POST` | `/api/parse` | Запустить ручной процесс парсинга. |
| `POST` | `/api/send-telegram` | Отправить новость (Body: `{"news_id": 1}`). |
| `POST` | `/api/auto-send/start` | Запустить фоновый процесс авто-парсинга. |
| `POST` | `/api/auto-send/stop` | Остановить фоновый процесс. |

---

## 📂 Структура Проекта

```text
news_parsing_eng_bot/
├── app/
│   ├── main.py           # Точка входа, API роуты, логика авто-парсинга
│   ├── database.py       # Работа с SQLite (init, add, get, delete)
│   ├── scraper.py        # Логика Playwright, классы парсеров для сайтов
│   ├── translator.py     # Клиент к LibreTranslate API
│   ├── telegram_bot.py   # Функции отправки сообщений в Telegram
│   └── templates/
│       └── index.html    # Frontend (HTML + JS + CSS)
├── data/                 # Директория для хранения БД (news.db)
├── Dockerfile            # Инструкции сборки Python-окружения
├── docker-compose.yml    # Конфигурация сервисов (App + DB + Translator)
└── requirements.txt      # Зависимости Python
```

---

## ❓ Устранение Неполадок

**1. "Переводчик недоступен" в статусе**
*   Убедитесь, что контейнер `libretranslate` запущен (`docker ps`).
*   При первом запуске ему нужно время на загрузку языковых моделей. Подождите 1-2 минуты.

**2. Новости не парсятся (Found: 0)**
*   Проверьте доступность сайтов. Наведите на статус "Найдено", чтобы увидеть, какие сайты `Offline`.
*   Возможно, сайты изменили верстку. В этом случае требуется обновление селекторов в `scraper.py`.

**3. Ошибка авторизации Telegram** 
*   Проверьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`.
*   Убедитесь, что бот **добавлен администратором** в ваш канал.